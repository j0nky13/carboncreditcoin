export default function WhyEnforcementMatters() {
  return (
    <section className="max-w-4xl mx-auto space-y-6 border-l border-primary/40 pl-6">
      <h2 className="text-2xl md:text-3xl font-semibold">
        Why enforcement matters
      </h2>

      <p className="text-white/80 leading-relaxed">
        Systems only work when rules are unavoidable. Optional impact is not
        impact — it is marketing.
      </p>

      <p className="text-white/75 leading-relaxed">
        CarbonChain enforces outcomes at the protocol level, ensuring that
        participation itself triggers measurable, auditable effects.
      </p>
    </section>
  );
}