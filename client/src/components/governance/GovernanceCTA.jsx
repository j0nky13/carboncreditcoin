export default function GovernanceCTA() {
  return (
    <section className="max-w-4xl mx-auto text-center space-y-6 pt-20 pb-6">
      {/* Divider */}
      <div className="mx-auto w-24 h-px bg-white/10 mb-10" />

      {/* Headline */}
      <h2 className="text-2xl md:text-3xl font-semibold tracking-tight">
        Transparent governance by design
      </h2>

      {/* Core message */}
      <p className="text-white/70 leading-relaxed max-w-xl mx-auto">
        Governance is one layer of CarbonChain — not the foundation it stands on.
        Core economic rules, environmental enforcement, and security guarantees
        remain intact regardless of sentiment or voting outcomes.
      </p>

      {/* Reinforcing line */}
      <p className="text-sm text-white/55 italic max-w-lg mx-auto">
        Stewardship guides the network forward. The protocol defines its limits.
      </p>
    </section>
  );
}